spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/onlineshopdb
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 99
  
nginx:
  location: http://localhost/images/
image:
  baseDir: D:/nginx-1.16.2/html/imags/

